##### ReadMe ########

Following promptsd are shown to user to enter the directory name, file type and string to search. If directory name is not entered, then search will be done in the current directory from where the script is being executed.

Enter the directory in which you want to search for string/word, for e.g. /tmp etc.:  /Downloads
Enter the File Type in which you want to search for string for e.g. .log or .txt etc.: .txt
Enter the search string : test

In above example we entered /Downloads as directory, .txt as file type and test as string to search.

if tbe script files the match, it will display it as follows:

patching.log test completed all pre-patch checks



